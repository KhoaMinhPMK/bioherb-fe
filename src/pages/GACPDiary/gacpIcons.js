/**
 * GACP Icon Map — Maps iconName strings to Lucide React components.
 * Centralized so all GACP components use Lucide icons consistently.
 */
import {
    Sprout,
    Mountain,
    Layers,
    Droplets,
    Filter,
    Recycle,
    FlaskConical,
    Pill,
    Trash2,
    ClipboardList,
    Bug,
    Wheat,
    FolderArchive,
    Wrench,
    AlertTriangle,
    Shovel,
    Leaf,
    Settings,
} from 'lucide-react';

const GACPIconMap = {
    Sprout,
    Mountain,
    Layers,
    Droplets,
    Filter,
    Recycle,
    FlaskConical,
    Pill,
    Trash2,
    ClipboardList,
    Bug,
    Wheat,
    FolderArchive,
    Wrench,
    AlertTriangle,
    Shovel,
    Leaf,
    Settings,
};

export default GACPIconMap;
